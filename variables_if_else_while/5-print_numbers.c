#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase, except for q and e.
 *
 * Return: Always 0.
 */

int main(void)
{
	int i;/*Declaraci√n de mi variable*/

	for (i = 0; i < 10; i++)
{
	printf("%d", i);
}
	printf("\n");

	return (0);
}
