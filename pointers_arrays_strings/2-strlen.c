#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - cuenta
 *
 * @s: puntero hacia una variable del tipo char
 *
 * Return: Devuelve la longitud del string.
 */


int _strlen(char *s)
{
	int len = 0;

	for (len = 0; s[len] != '\0'; len++)
	{

	}

	return (len);
}
